function navegacion(){//navegaci&oacute;n menuvar link = $("#menu ul li.activo").find("a").attr("rel");if(link != "") {$("#submenu .submenu-"+link).toggleClass('toggled');$("#submenu .submenu-"+link).slideToggle();}//navegaci&oacute;n submenu$("#submenu ul li").click(function(event) {event.preventDefault();$("#submenu ul li.activo").removeClass('activo');$(this).addClass('activo');var link = $(this).find("a").attr("rel");cargarContenido(link);});}function cargarContenido(link) {var pagina = link+".html";$("#cuerpo").load(pagina, function(){activarContenido();});return false;}function activarContenido(){//Si existe la imagen con texto, activamos efectosif($("#imagen .texto-imagen").length){$("#imagen .texto-imagen").show(); //mostramos el texto$("#imagen .texto-imagen").animate({ opacity: 0.7 }, 1 ); //le damos opacidad para que se vea$("#miniaturas #imgs li:first").addClass('active');  //aplicamos clase active al primer elemento$('ul#imgs li').not(".active").not(".slider li").css({opacity:".5"}); //quitamos opacidad al resto//Activamos el slideshowactivarSlider();}}function activarSlider(){$(".slider").easySlider({auto: true, continuous: false,controlsShow: false,prevId: 'prevBtn',prevText: 'Anterior',nextId: 'nextBtn',nextText: 'Siguiente',controlsShow:true,controlsBefore:'<div id="controls">',controlsAfter:'</div>',speed: 3000});}//Función para cargar la imagen principal y los textos desde las miniaturas del lateral$("#miniaturas #imgs li").live('click', function(event){event.preventDefault();//var imgAlt = $(this).find('img').attr("alt"); //Cogemos el Alt de la imagenvar imgList = $(this).find('.slider').html(); //lista de imagenesvar imgTxt = $(this).find('.texto-mini').html(); //Texto que aparecerá sobre la imagenvar imgTxtWidth = 100; // Ancho del textoif ($(this).is(".active")) {  //Si ya está activo...return false; // Que no deje clicar} else {//Animamos el bloque de texto$("#imagen .texto-imagen").animate({ opacity: 0, marginRight: -imgTxtWidth }, 250 , function() {$("#imagen .texto-imagen").html(imgTxt).animate({ opacity: 0.7,marginRight: "0" }, 250 );$("#imagen .slider").replaceWith('<div class="slider">'+imgList+'</div>');$("#controls").remove();activarSlider();});}$("#miniaturas ul li").removeClass('active'); //Quitamos clase 'active'$(this).addClass('active');  //Añadimos 'active' sólo al seleccionado$('ul#imgs li').not(".active").not(".slider li").css({opacity:".5"}); //Quitamos opacidad a los no seleccionadosreturn false;});$("#miniaturas #imgs li").live('mouseenter', function(){ //Al pasar por encima...$(this).fadeTo(500,1); //Opacidad -> 1});$("#miniaturas #imgs li").live('mouseleave', function() {$(this).not('.active.').fadeTo(500,.5);});